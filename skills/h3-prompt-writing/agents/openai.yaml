interface:
  display_name: "MiniMax H3 Prompt Writing"
  short_description: "Write H3 base and full-reference video prompts; accepts director-card or minimax-h3 handoffs"
  default_prompt: "Use $h3-prompt-writing to rewrite this request (or the structured handoff) into a MiniMax H3 generation prompt. Do not connect to ComfyUI or submit a job."
