# 贡献指南 | Contributing

欢迎 issue 和 PR。这是一套互相引用的技能流水线，改动前请先了解它的两条结构约定，避免破坏现有约定。

## 结构约定

1. **单一真源**：每类专业规则只有一份权威文件（如 H3 提示词结构、生图坑清单）。其他技能引用它，不复述它。新增规则时先问：它属于哪个现有真源文件？只有在开辟全新领域时才新建文件。
2. **技能间用显式契约交接**：例如 `h3-prompt-writing` 与 `minimax-h3` 之间的 handoff schema。改动任何一边的字段名时，必须同步另一边。

## 改动类型与要求

- **H3 语法类**（`h3-prompt-writing`、`minimax-h3`）：H3 迭代快，PR 请附 [MiniMax 官方仓库](https://github.com/MiniMax-AI/MiniMax-H3) 的最新依据（文件链接或提交）。
- **新技能**：说明它归哪个阶段调用、与现有技能的边界、以及编排器（`short-drama-production`）是否需要新增交接点。

## 提交前自测

至少跑通一条最小链路：一集剧本与台词时间轴 → 少量片段提示词 → 片段生成。

## 致谢义务

若你的改动借鉴了其他开源项目，请同步更新 `NOTICE`，保留上游版权声明。
